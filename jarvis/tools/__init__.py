"""Tool plugin system: drop a module in this package, it auto-registers.
Each tool declares schema, permission level, destructive flag; gating per
config/permissions.yaml. NOT YET IMPLEMENTED — Phase 3."""
