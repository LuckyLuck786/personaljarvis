# Secrets — never commit
.env
*.env
!.env.example
!deploy/**/*.env.example

# Operator-local config overrides (model choice, speed tuning) — kept out of
# git so they survive `git pull` and reinstalls without conflicts.
config/*.local.yaml

# Python
__pycache__/
*.pyc
*.egg-info/
.venv/
.pytest_cache/
dist/
build/

# Runtime data (SQLite, vector store, logs live outside the repo by default,
# but guard against accidental in-repo data dirs)
var/
data/
*.db
*.db-wal
*.db-shm
logs/

# OS
.DS_Store
.claude/launch.json
